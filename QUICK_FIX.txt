╔═══════════════════════════════════════════════════════════════╗
║           🔧 QUICK FIX FOR INSTALLATION ERROR                 ║
╚═══════════════════════════════════════════════════════════════╝

ERROR: "Microsoft Visual C++ 14.0 or greater is required"

ROOT CAUSE: Missing Visual C++ Build Tools needed to compile PyAV

╔═══════════════════════════════════════════════════════════════╗
║                  ✅ SOLUTION (Choose One)                     ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│ OPTION 1: Automated Fix (EASIEST - Recommended)              │
└───────────────────────────────────────────────────────────────┘

Open PowerShell AS ADMINISTRATOR and run:

    .\fix_install.bat

This will:
  ✓ Check for required tools
  ✓ Install Visual C++ Build Tools if missing
  ✓ Clean and recreate virtual environment
  ✓ Install all dependencies with fallbacks


┌───────────────────────────────────────────────────────────────┐
│ OPTION 2: Manual Fix (If you want control)                   │
└───────────────────────────────────────────────────────────────┘

STEP 1: Install Build Tools (PowerShell as Admin)

    choco install visualstudio2022buildtools -y

    ⏱️ Time: 10-15 minutes
    💾 Size: ~2-3 GB download

STEP 2: Restart PowerShell

STEP 3: Clean Install

    cd "D:\remote_browser_demo\New Setup Streaming"
    Remove-Item -Recurse -Force venv
    python -m venv venv
    .\venv\Scripts\Activate.ps1
    pip install --upgrade pip
    pip install -r requirements.txt

STEP 4: Run

    python app.py


┌───────────────────────────────────────────────────────────────┐
│ OPTION 3: Quick Workaround (Skip av package temporarily)     │
└───────────────────────────────────────────────────────────────┘

If you want to test without waiting for Build Tools:

    .\venv\Scripts\Activate.ps1
    
    # Install only packages that work without compilation
    pip install flask flask-cors selenium opencv-python numpy
    
    # Try aiortc (might work with pre-built wheel)
    pip install aiortc

    # Temporarily modify app.py to not import av
    # (You'll need to adjust the video frame creation)

⚠️  This won't fully work without 'av', but you can test Flask routes


┌───────────────────────────────────────────────────────────────┐
│ OPTION 4: Use Conda (Alternative package manager)            │
└───────────────────────────────────────────────────────────────┘

Conda has pre-compiled binaries:

    choco install miniconda3 -y
    
    # Close and reopen PowerShell
    
    conda create -n browser-stream python=3.10
    conda activate browser-stream
    conda install -c conda-forge ffmpeg
    pip install -r requirements.txt

╔═══════════════════════════════════════════════════════════════╗
║                    ⚡ FASTEST PATH                            ║
╚═══════════════════════════════════════════════════════════════╝

If you have admin access:

1. Open PowerShell AS ADMINISTRATOR

2. Run:
   .\fix_install.bat

3. If it says "restart computer", do that

4. After restart, run:
   .\run.bat

5. Open browser:
   http://localhost:5000

╔═══════════════════════════════════════════════════════════════╗
║                  📊 WHAT'S HAPPENING?                         ║
╚═══════════════════════════════════════════════════════════════╝

The 'av' (PyAV) package wraps FFmpeg C++ libraries.
On Windows, it needs to compile C++ code, which requires:

  ❌ Visual C++ compiler (cl.exe)      ← YOU'RE MISSING THIS
  ❌ Windows SDK
  ❌ Build tools

Once you install Visual Studio Build Tools, you'll have all three.

╔═══════════════════════════════════════════════════════════════╗
║                   ✅ VERIFICATION                             ║
╚═══════════════════════════════════════════════════════════════╝

After installing, verify with:

    # Check if compiler is available
    where cl

    # Test imports
    python -c "import av; print('✅ av works!')"
    python -c "import aiortc; print('✅ aiortc works!')"

╔═══════════════════════════════════════════════════════════════╗
║                   📞 STILL STUCK?                             ║
╚═══════════════════════════════════════════════════════════════╝

Check these files for detailed help:
  - FIX_INSTALLATION.md    (Complete troubleshooting guide)
  - SETUP_GUIDE.md         (Step-by-step Windows setup)
  - README.md              (General troubleshooting)

Common issues:
  • "choco not found" → Install Chocolatey first
  • "Permission denied" → Run PowerShell as Administrator  
  • "Port 5000 in use" → Change port in app.py
  • Still failing → Try Python 3.10 instead of 3.12

╔═══════════════════════════════════════════════════════════════╗
║              🎯 RECOMMENDED: Use fix_install.bat              ║
╚═══════════════════════════════════════════════════════════════╝

It handles everything automatically! Just run:

    .\fix_install.bat

(as Administrator)

